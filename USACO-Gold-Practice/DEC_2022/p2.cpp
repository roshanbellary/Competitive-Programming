#include <bits/stdc++.h>
using namespace std;
const int MN = 2000;
int h[MN], N, Q;
bool check(int i, int j){

}
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    cin >> N;
    for (int i=0;i<N;i++) cin >> h[i];
    cin >> Q;
    while (Q--){
        int x, y; cin >> x >> y;
        h[--x]+=y; int res = N-1;
        int l=0, r=1;
        for (int i=2;i<N;i++){
            if ((i-r)*)
        }
    }
}